packages:
  - "apps/*"
  - "packages/*"

# Pre-approve native build scripts pnpm would otherwise skip (workerd downloads
# the platform binary; esbuild builds native bindings; better-sqlite3 compiles).
allowBuilds:
  esbuild: true
  workerd: true
  better-sqlite3: true
